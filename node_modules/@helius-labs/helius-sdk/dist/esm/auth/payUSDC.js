import { TREASURY, USDC_MINT, PAYMENT_AMOUNT } from './constants.js';
import { buildAndSendTokenTransfer } from './buildTokenTransfer.js';

async function payUSDC(secretKey) {
    return buildAndSendTokenTransfer({
        secretKey,
        recipientAddress: TREASURY,
        mintAddress: USDC_MINT,
        amount: PAYMENT_AMOUNT,
    });
}

export { payUSDC };
//# sourceMappingURL=payUSDC.js.map
