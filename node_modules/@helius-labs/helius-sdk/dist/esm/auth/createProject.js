import { authRequest } from './utils.js';

async function createProject(jwt, userAgent) {
    return authRequest("/projects/create", {
        method: "POST",
        headers: { Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({}),
    }, userAgent);
}

export { createProject };
//# sourceMappingURL=createProject.js.map
