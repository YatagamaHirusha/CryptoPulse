import { authRequest } from './utils.js';

async function fetchOpenPayPriceIds(jwt, userAgent) {
    const configs = await authRequest("/dev-portal/configs", {
        method: "GET",
        headers: { Authorization: `Bearer ${jwt}` },
    }, userAgent);
    return configs.openPay.priceIds;
}

export { fetchOpenPayPriceIds };
//# sourceMappingURL=devPortalConfigs.js.map
