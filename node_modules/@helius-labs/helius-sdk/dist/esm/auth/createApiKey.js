import { authRequest } from './utils.js';

async function createApiKey(jwt, projectId, walletAddress, userAgent) {
    return authRequest(`/projects/${projectId}/add-key`, {
        method: "POST",
        headers: { Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({ userId: walletAddress }),
    }, userAgent);
}

export { createApiKey };
//# sourceMappingURL=createApiKey.js.map
