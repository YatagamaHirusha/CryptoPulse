'use strict';

var constants = require('./constants.cjs');

function isOpenPayPlan(plan) {
    return constants.OPENPAY_PLANS.includes(plan);
}
function buildEndpoints(apiKey) {
    return {
        mainnet: `https://mainnet.helius-rpc.com/?api-key=${apiKey}`,
        devnet: `https://devnet.helius-rpc.com/?api-key=${apiKey}`,
    };
}

exports.buildEndpoints = buildEndpoints;
exports.isOpenPayPlan = isOpenPayPlan;
//# sourceMappingURL=signupHelpers.cjs.map
