'use strict';

var utils = require('./utils.cjs');

async function createProject(jwt, userAgent) {
    return utils.authRequest("/projects/create", {
        method: "POST",
        headers: { Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({}),
    }, userAgent);
}

exports.createProject = createProject;
//# sourceMappingURL=createProject.cjs.map
