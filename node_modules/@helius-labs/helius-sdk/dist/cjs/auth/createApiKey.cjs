'use strict';

var utils = require('./utils.cjs');

async function createApiKey(jwt, projectId, walletAddress, userAgent) {
    return utils.authRequest(`/projects/${projectId}/add-key`, {
        method: "POST",
        headers: { Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({ userId: walletAddress }),
    }, userAgent);
}

exports.createApiKey = createApiKey;
//# sourceMappingURL=createApiKey.cjs.map
