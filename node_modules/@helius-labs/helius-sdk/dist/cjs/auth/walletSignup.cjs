'use strict';

var utils = require('./utils.cjs');

async function walletSignup(message, signature, address, userAgent) {
    return utils.authRequest("/wallet-signup", {
        method: "POST",
        body: JSON.stringify({ message, signature, userID: address }),
    }, userAgent);
}

exports.walletSignup = walletSignup;
//# sourceMappingURL=walletSignup.cjs.map
