'use strict';

var utils = require('./utils.cjs');

async function fetchOpenPayPriceIds(jwt, userAgent) {
    const configs = await utils.authRequest("/dev-portal/configs", {
        method: "GET",
        headers: { Authorization: `Bearer ${jwt}` },
    }, userAgent);
    return configs.openPay.priceIds;
}

exports.fetchOpenPayPriceIds = fetchOpenPayPriceIds;
//# sourceMappingURL=devPortalConfigs.cjs.map
