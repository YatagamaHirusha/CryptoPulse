'use strict';

function loadKeypair(bytes) {
    if (bytes.length !== 64) {
        throw new Error("Invalid keypair format. Expected Uint8Array of 64 bytes (Solana CLI format).");
    }
    const secretKey = new Uint8Array(bytes);
    const publicKey = secretKey.slice(32);
    return { publicKey, secretKey };
}

exports.loadKeypair = loadKeypair;
//# sourceMappingURL=loadKeypair.cjs.map
