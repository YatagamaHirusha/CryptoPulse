'use strict';

var utils = require('./utils.cjs');

async function getProject(jwt, id, userAgent) {
    return utils.authRequest(`/projects/${id}`, {
        method: "GET",
        headers: { Authorization: `Bearer ${jwt}` },
    }, userAgent);
}

exports.getProject = getProject;
//# sourceMappingURL=getProject.cjs.map
