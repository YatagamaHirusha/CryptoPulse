'use strict';

var computeBudget = require('@solana-program/compute-budget');

const makeGetComputeUnits = (raw) => {
    const estimateFn = computeBudget.estimateComputeUnitLimitFactory({
        rpc: raw,
    });
    return async (message, { min = 1000, bufferPct = 0.1 } = {}) => {
        const rawUnits = await estimateFn(message);
        const units = Number(rawUnits);
        return Math.max(min, Math.ceil(units * (1 + bufferPct)));
    };
};

exports.makeGetComputeUnits = makeGetComputeUnits;
//# sourceMappingURL=getComputeUnits.cjs.map
