'use strict';

var fetchTipFloor = require('./fetchTipFloor.cjs');
var lamports = require('./lamports.cjs');

/**
 * Returns a SOL value for the tip:
 *  • current 75‑percentile landed‑tip floor, **or**
 *  • fallback 0.001 SOL (Sender minimum)
 */
const determineTipSol = async (swqosOnly) => {
    const floorSol = await fetchTipFloor.fetchTipFloor75th();
    const minSol = swqosOnly ? 0.0005 : 0.001;
    const chosenSol = Math.max(floorSol ?? minSol, minSol);
    return lamports.solToLamports(chosenSol);
};

exports.determineTipSol = determineTipSol;
//# sourceMappingURL=determineTip.cjs.map
