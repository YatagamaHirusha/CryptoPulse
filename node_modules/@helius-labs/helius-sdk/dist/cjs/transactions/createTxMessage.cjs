'use strict';

var kit = require('@solana/kit');

const createTxMessage = ({ version, feePayer, lifetime, instructions, }) => {
    return kit.pipe(kit.createTransactionMessage({ version }), (m) => lifetime ? kit.setTransactionMessageLifetimeUsingBlockhash(lifetime, m) : m, (m) => typeof feePayer === "string"
        ? kit.setTransactionMessageFeePayer(feePayer, m)
        : kit.setTransactionMessageFeePayerSigner(feePayer, m), (m) => kit.appendTransactionMessageInstructions(instructions, m));
};

exports.createTxMessage = createTxMessage;
//# sourceMappingURL=createTxMessage.cjs.map
