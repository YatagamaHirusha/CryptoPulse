'use strict';

var stake = require('@solana-program/stake');
var types = require('./types.cjs');

const makeGetWithdrawInstruction = () => (owner, stakeAccount, destination, lamports) => stake.getWithdrawInstruction({
    stake: stakeAccount,
    recipient: destination,
    clockSysvar: types.SYSVAR_CLOCK,
    stakeHistory: types.SYSVAR_STAKE_HISTORY,
    withdrawAuthority: owner,
    args: lamports,
});

exports.makeGetWithdrawInstruction = makeGetWithdrawInstruction;
//# sourceMappingURL=getWithdrawInstruction.cjs.map
