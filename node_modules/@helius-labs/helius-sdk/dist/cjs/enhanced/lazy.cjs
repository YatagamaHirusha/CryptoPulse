'use strict';

const makeEnhancedTxClientLazy = (apiKey, network = "mainnet", userAgent) => {
    const load = async () => {
        const { makeEnhancedTxClientEager } = await Promise.resolve().then(function () { return require('./client.eager.cjs'); });
        return makeEnhancedTxClientEager(apiKey, network, userAgent);
    };
    return {
        getTransactions: async (params) => (await load()).getTransactions(params),
        getTransactionsByAddress: async (params) => (await load()).getTransactionsByAddress(params),
    };
};

exports.makeEnhancedTxClientLazy = makeEnhancedTxClientLazy;
//# sourceMappingURL=lazy.cjs.map
